;; MyEmacs configuration options.

;; Spaces instead of tabs
(setq indent-tabs-mode nil)

;; Newline at the end of document
(setq require-final-newline t)

;; Display line numbers
(global-linum-mode t)
(line-number-mode t)
